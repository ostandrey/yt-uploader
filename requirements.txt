# Coin Wire — production dependencies

requests==2.31.0
python-dotenv==1.0.0
pyyaml==6.0.1
feedparser==6.0.10
pillow==10.0.0

# Video / audio
edge-tts>=7.2.0
imageio-ffmpeg>=0.5.0
faster-whisper>=1.0.0
# Optional B-roll YOLO QA on fill
ultralytics>=8.0.0
opencv-python-headless>=4.8.0

# YouTube OAuth + upload
google-api-python-client==2.100.0
google-auth==2.23.0
google-auth-oauthlib==1.0.0

# Scheduler (Railway worker)
apscheduler==3.10.4

# S3-compatible public media host (Instagram / Threads need a public video URL)
boto3>=1.34.0
